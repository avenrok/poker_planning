import 'package:flutter/material.dart';

class AutorizationView extends StatelessWidget {
  const AutorizationView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}